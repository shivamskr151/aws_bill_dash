PORT=8787
CORS_ORIGIN=http://localhost:5173
AWS_REGION=us-east-1

# Credentials: do NOT paste secrets here.
# Use the standard AWS SDK credential chain:
# - export AWS_PROFILE=your-profile
# - or export AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY (short-lived recommended)
# - or use SSO / IAM role (best)

